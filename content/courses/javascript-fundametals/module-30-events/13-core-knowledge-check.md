# Core Knowledge Check

You've covered event-driven programming, event types, `addEventListener`, event
objects, bubbling, capturing, delegation, default actions, custom events, form
events, listener options, and cleanup basics.

This quiz checks the core concepts needed to handle browser events confidently.

> You need to score **70% or higher** to complete this lesson.
> You can retake the quiz as many times as you like - your best score counts.
