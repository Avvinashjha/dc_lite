# Core Knowledge Check

You've covered the Node.js runtime, running scripts, globals, modules, npm, built-in modules, file system operations, environment variables, command-line arguments, async APIs, streams, events, and HTTP basics.

This quiz checks the core ideas behind using JavaScript in the Node.js environment.

> You need to score **70% or higher** to complete this lesson.
> You can retake the quiz as many times as you like - your best score counts.
