# Core Knowledge Check

You've covered the browser runtime, `window`, `document`, the DOM, element selection and modification, events, script loading, browser APIs, storage, navigation, rendering, compatibility, and DevTools.

This quiz checks the core concepts needed to work with JavaScript in the browser environment.

> You need to score **70% or higher** to complete this lesson.
> You can retake the quiz as many times as you like - your best score counts.
