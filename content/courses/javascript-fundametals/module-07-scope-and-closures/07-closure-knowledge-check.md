# Closure Knowledge Check

You've covered closures, closure scope, shared private state, shadowing, and loop behavior with `var` and `let`.

This quiz checks whether you can predict what closures remember, when state is shared or isolated, and how scope choices affect callback behavior.

> You need to score **70% or higher** to complete this lesson.
> You can retake the quiz as many times as you like - your best score counts.
