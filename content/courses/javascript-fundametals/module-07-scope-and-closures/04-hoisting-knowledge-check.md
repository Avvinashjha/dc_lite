# Hoisting Knowledge Check

Hoisting can feel tricky because different declarations behave differently before their declaration line runs.

This quiz focuses on the common edge cases: `var`, `let`, `const`, temporal dead zone behavior, function declarations, function expressions, and scope boundaries.

> You need to score **70% or higher** to complete this lesson.
> You can retake the quiz as many times as you like - your best score counts.
