# Core Knowledge Check

You've covered JavaScript modules, `import` and `export`, default and named exports, browser and Node module loading, re-exports, side effects, and dynamic imports.

This quiz checks the core syntax and concepts you need before using modules in real projects.

> You need to score **70% or higher** to complete this lesson.
> You can retake the quiz as many times as you like - your best score counts.
