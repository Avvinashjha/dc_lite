# Core Knowledge Check

You've covered synchronous versus asynchronous code, callbacks, callback hell, promises, `async` / `await`, async error handling, and parallel async tasks.

This quiz checks the core concepts needed to work with asynchronous JavaScript.

> You need to score **70% or higher** to complete this lesson.
> You can retake the quiz as many times as you like - your best score counts.
