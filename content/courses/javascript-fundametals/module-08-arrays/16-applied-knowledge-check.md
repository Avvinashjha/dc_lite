# Applied Knowledge Check

This final quiz focuses on the tricky parts of arrays: mutation, shallow copies, callback return values, default sorting, object references, flattening depth, and nested array access.

Read each code example carefully. Many array bugs come from using the right method in the wrong way.

> You need to score **70% or higher** to complete this lesson and finish the module.
> You can retake the quiz as many times as you like - your best score counts.
