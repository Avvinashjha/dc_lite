# Core Knowledge Check

You've covered the core rules of `this`, how context is determined, why context can be lost, and how arrow functions behave differently from regular functions.

This quiz checks whether you understand the foundation before moving into applied `bind`, `call`, and `apply` scenarios.

> You need to score **70% or higher** to complete this lesson.
> You can retake the quiz as many times as you like - your best score counts.
