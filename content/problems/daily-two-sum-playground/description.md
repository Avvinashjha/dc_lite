# Problem

Given an integer array `nums` and an integer `target`, return the indices of two distinct numbers such that they add up to `target`.

You may assume exactly one valid pair exists.

## Why this matters

This is the canonical introduction to the hash map lookup pattern. It appears in many daily interview-style tasks and helps with moving from brute-force `O(n^2)` to linear `O(n)`.
