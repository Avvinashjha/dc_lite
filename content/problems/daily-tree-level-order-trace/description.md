# Problem

Given the root of a binary tree, return the level order traversal of its nodes' values from left to right.

Return one list per depth.

## Hint

When using BFS, the queue size before processing a level tells you exactly how many nodes belong to that level.
