Given a binary tree, return the maximum width — the maximum number of nodes between any two nodes at the same level (including nulls between them).

**Example:** root = [1,3,2,5,3,null,9] → Output: 4
